name: motorsurveyapp
description: Motor Insurance Survey App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.3.6
  sqflite: ^2.2.8
  path: ^1.8.3
  camera: ^0.10.5+5
  geolocator: ^9.0.2
  google_ml_kit: ^0.16.0
  http: ^0.13.5
  file_picker: ^5.2.5
  # firebase_core: ^2.15.1
  # firebase_storage: ^11.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true